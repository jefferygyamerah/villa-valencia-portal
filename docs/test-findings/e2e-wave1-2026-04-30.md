# E2E Wave 1 Verification — 2026-04-30

Source plan: `docs/e2e-fix-orchestration-plan-2026-04-30.md`
Source scope log: `docs/website-fix-log.md`
Host: `openclaw-pi5`

## Wave 1 changes

- Removed `Ingreso residentes` from public Accessos.
- Tightened public Accessos to an intentional two-card administration/junta layout.
- Moved the app install prompt container above the home operating lens.
- Hid duplicated home module cards on logged-in phone views where fixed bottom nav exists.
- Removed the `Límite de confianza` product-style card from the shared home operating lens.

## Focused verification

```bash
cd /home/jeffery/Adwen-Tech/apps/villa-valencia-portal/e2e
npx playwright test tests/portal-home.spec.ts tests/suite-routes.spec.ts --grep-invert "mapa only allows route points"
```

Result: **18 passed / 0 failed**.

## Full local verification

```bash
cd /home/jeffery/Adwen-Tech/apps/villa-valencia-portal/e2e
npm test
```

Result after Wave 1: **35 passed / 1 failed**.

The only failure is the same baseline map guardrail failure documented in `docs/test-findings/e2e-baseline-2026-04-30.md`:

```text
tests/suite-routes.spec.ts › mapa only allows route points inside Villa Valencia boundary
Expected #mapa-new-save to be visible, but the element was not found.
```

## Delivery decision

Wave 1 is safe to keep locally. No new failures were introduced. Hold production deployment until Jeff decides whether to ship the UI cleanup with the known map baseline failure or fix the map guardrail first.
